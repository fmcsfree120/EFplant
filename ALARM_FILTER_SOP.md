# EFplant 警報過濾器 SOP

## 適用範圍

同一套警報過濾器必須同時套用於：

- Dashboard 的「TOP 10 高風險設備」與「TOP 10 警報復歸耗時」。
- 管理週報的 `alarm_risk`、健康度排序及所有以警報資料計算的評比。

過濾必須在警報資料完成欄位清理後、任何期間統計、分組、排名或評分前執行；被排除的紀錄不得以零警報、資料缺失或任何替代值重新納入評比。

## 排除條件

僅以同一筆紀錄的 `ALM_DESCR`（警報描述）判定。符合下列任一條件即排除：

1. 同時包含 `MAU` 與 `液位`。
2. 包含 `KW`、`供電`、`用電`、`功率` 或 `S2A電力` 任一用詞。
3. 同時包含 `溫度`、`濕度`、`溼度` 或 `室壓` 任一用詞，以及 `無塵室` 或 `CR` 任一用詞。
4. 包含 `再生` 或 `樹脂`。
5. 包含 `變頻回授`。
6. 同時包含 `區` 與 `溫度`。
7. 同時包含 `區` 與 `濕度` 或 `溼度`。
8. 包含大寫英文字元 `L` 或 `H`。此條為描述文字的字面比對，不以警報狀態欄位替代。

## 實作規範

1. 共用規則維護在 `alarm_filter.py` 的 `filter_alarm_records()`。
2. Dashboard 與週報都必須呼叫此函式，禁止各自複製或改寫規則。
3. 濾除只影響呈現與評比；原始警報 CSV 必須保留，供稽核與日後調整條件使用。

## 驗證與發布

1. 執行 `python -m py_compile alarm_filter.py generate_dashboard.py generate_weekly_analysis.py`。
2. 重建 Dashboard 與週報 JSON，確認被排除描述不出現在兩個 TOP 10 看板、`alarm_risk`、週報健康度排序或相關警報計算中。
3. 前端變更完整遵循 `FRONTEND_UPDATE_SOP.md`：更新快取版本、重建、暫存必要部署檔、提交、推送並驗證正式 GitHub Pages。
